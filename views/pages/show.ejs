<%- layout("/layouts/boilerplate") %>


<body>
    <div class="row mt-3">
        <div class="col-6 offset-3">
    <div class="card card-main">
        <div class="card-header"><%=users.first_name%> <%=users.last_name%>
         
          <form method="get" action="/users/<%=users._id%>/edit">
            <button class="btn btn-dark btn-edit">edit</button>
          </form>
         </div>
        <div class="card-body">
          <h5 class="card-title">id :<%=users.id %></h5>
          <p class="card-text"><%=users.email%></p>
          <p class="card-text"><%=users.domain%></p>
          <p class="card-text"><%=users.available%></p>


          <form method="get" action="/users/cart">
            <button class="btn btn-warning btn-joined">Join Team</button>

          </form>
          
          

        </div>
      </div>
      </div>
      </div>
</body>